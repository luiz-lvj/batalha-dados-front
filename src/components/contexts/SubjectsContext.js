import { createContext } from "react";

export const subjects = ["Matemática", "Português", "Geografia", "História", "Física", "Química", "Filosofia", "Sociologia"];

const SubjectsContext = createContext();

export default SubjectsContext;